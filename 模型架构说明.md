# 3D-MedDiffusion 心肌病数据生成模型 — 架构说明

## 摘要

本文档详细描述了基于 3D-MedDiffusion 的心肌病多模态 MRI 数据生成模型的完整架构，从输入数据到输出合成数据的全流程。

---

## 1. 问题定义

### 1.1 目标

为罕见心肌病（RCM、ARVC、DCM、HCM、LVNC）生成高质量的 3D 心脏 MRI 合成数据，用于：
- 数据增强
- 模型训练
- 临床研究

### 1.2 输入数据

每个患者包含 3 种模态的 MRI 数据：

| 模态 | 原始尺寸 | 描述 |
|------|----------|------|
| cine4ch | (512, 512, 25) | 长轴电影序列，25帧 |
| cinesax | (512, 512, 25) × 3 | 短轴电影序列，3个切片，每个25帧 |
| lgesax | (256, 256, 6) | 延迟增强序列，6层切片 |

### 1.3 输出数据

每个患者生成与输入格式完全一致的数据：

| 模态 | 输出尺寸 | 输出文件数 | 文件名 |
|------|----------|------------|--------|
| cine4ch | (512, 512, 1) | 1 | cine_4ch_mid.nii.gz |
| cinesax | (512, 512, 1) | 1 | cine_sax_mid.nii.gz |
| lgesax | (256, 256, 1) | 2 | lge_sax_mid.nii.gz, lge_sax_mid6.nii.gz |

---

## 2. 数据预处理

### 2.1 帧选择（Temporal Selection）

电影序列（cine）包含多个时间帧，需要选择代表性帧：

**策略：mid_only（默认）**
```
输入: (H, W, D, T) = (512, 512, 1, 25)
输出: (H, W, D) = (512, 512, 1)
方法: 选择中间帧 T//2 = 12
```

**其他策略：**
- `average`: 平均所有帧
- `stack`: 保留所有帧作为通道
- `first` / `last`: 选择首/末帧

### 2.2 切片选择（Slice Selection）

**cinesax 切片对齐：**
```
输入: 3个切片 [down, mid, up]，每个 (512, 512, 25)
策略: mid_only → 选择中间切片 (mid)
输出: (512, 512, 25) → 帧选择 → (512, 512, 1)
```

**lgesax 切片选择：**
```
输入: (256, 256, 6)
策略: mid_only → 选择中间切片 (slice 3)
输出: (256, 256, 1)
```

### 2.3 多 LGE 文件处理

每个患者可能有多个 LGE 文件夹（2-3个），处理方式：

```
发现: patient_dir 下所有以 "0_final_custom_lgesax" 开头的文件夹
加载: 每个文件夹中的 .nii.gz 文件
输出: 保留前2个 LGE 体积
  - lgesax: 第一个 LGE (通常是 mid slice)
  - lgesax2: 第二个 LGE (通常是 another slice)
```

### 2.4 归一化

将数据归一化到 [-1, 1] 范围：

```python
vol_normalized = 2.0 * (vol - vol_min) / (vol_max - vol_min) - 1.0
```

### 2.5 数据增强

训练时应用随机增强：

1. **随机翻转**
   - 沿轴 0 翻转（概率 0.5）
   - 沿轴 1 翻转（概率 0.5）

2. **随机旋转**
   - 90° 旋转（0°, 90°, 180°, 270°）
   - 仅在轴向平面（axes 0, 1）

### 2.6 Patch 提取

从预处理后的体积中随机裁剪固定大小的 patch：

```
输入: (512, 512, 1) 或 (256, 256, 1)
Patch 大小: 128 × 128 × 1
输出: (128, 128, 1)

如果数据 < patch_size: 零填充后裁剪
如果数据 > patch_size: 随机裁剪
```

---

## 3. 模型架构

### 3.1 整体架构

```
输入数据 → 预处理 → AutoEncoder (VQ-GAN) → Latent → BiFlowNet (Diffusion) → 生成 Latent → AutoEncoder Decoder → 输出数据
```

### 3.2 AutoEncoder (VQ-GAN)

**功能：** 将 3D 医学图像编码到潜在空间，并从潜在空间解码重建。

**架构参数：**

| 参数 | 值 | 说明 |
|------|-----|------|
| downsample | [8, 8, 8] | 各维度下采样倍数 |
| in_channels | 1 | 输入通道数（单模态） |
| out_channels | 1 | 输出通道数 |
| n_hiddens | 48 | 隐藏层通道数 |
| embedding_dim | 8 | 潜在空间嵌入维度 |
| n_codes | 8192 | 码本大小（VQ） |
| patch_size | 128 | 输入 patch 大小 |
| n_groups | 24 | 分组归一化组数 |

**编码过程：**
```
输入: (B, 1, 128, 128, 1)
    ↓ Encoder (8x downsample)
    ↓ Vector Quantization
输出: (B, 8, 16, 16, 1) — Latent 表示
```

**解码过程：**
```
输入: (B, 8, 16, 16, 1) — Latent 表示
    ↓ Decoder (8x upsample)
输出: (B, 1, 128, 128, 1) — 重建图像
```

**训练策略：**

Stage 1: 完整训练
- 学习率: 3e-4
- 损失函数: L1 + Perceptual + GAN
- 判别器启动步数: 20000

Stage 2: 微调 Decoder
- 学习率: 3e-5
- 从 Stage 1 checkpoint 恢复
- 固定 Encoder，仅训练 Decoder

### 3.3 BiFlowNet (扩散模型)

**功能：** 在潜在空间中学习数据分布，生成新的潜在表示。

**架构参数：**

| 参数 | 值 | 说明 |
|------|-----|------|
| image_size | [16, 16, 16] | 潜在空间尺寸 |
| in_channels | 8 | 输入通道数（= embedding_dim） |
| model_channels | 128 | 模型基础通道数 |
| out_channels | 8 | 输出通道数 |
| num_res_blocks | 2 | 残差块数量 |
| attention_resolutions | [4, 2, 1] | 注意力层分辨率 |
| num_heads | 4 | 注意力头数 |
| num_classes | 5 | 疾病类别数 |

**扩散过程：**

```
前向扩散 (Training):
    x_0 (clean latent) → x_1 → ... → x_T (noise)
    q(x_t | x_{t-1}) = N(x_t; √(1-β_t) x_{t-1}, β_t I)

反向生成 (Inference):
    x_T (noise) → x_{T-1} → ... → x_0 (clean latent)
    p_θ(x_{t-1} | x_t) = N(x_{t-1}; μ_θ(x_t, t), Σ_θ(x_t, t))
```

**训练参数：**

| 参数 | 值 | 说明 |
|------|-----|------|
| timesteps | 1000 | 扩散步数 |
| beta_schedule | cosine | beta 调度策略 |
| loss_type | l2 | 损失函数类型 |
| ema_decay | 0.995 | 指数移动平均衰减率 |

**采样策略：**

DDIM (Denoising Diffusion Implicit Models):
- 步数: 50（可调整）
- eta: 0.0（确定性采样）

### 3.4 类条件生成

模型支持类条件生成，通过疾病类别控制生成结果：

```python
# 疾病类别映射
disease_to_idx = {
    "ARVC": 0,
    "DCM": 1,
    "HCM": 2,
    "LVNC": 3,
    "RCM": 4
}

# 生成时指定疾病类别
noise = torch.randn(1, 8, 16, 16, 1)
disease_label = torch.tensor([4])  # RCM
generated_latent = diffusion_model.sample(noise, disease_label)
```

---

## 4. 生成流程

### 4.1 完整生成流程

```
1. 初始化
   ├── 加载训练好的 AutoEncoder checkpoint
   ├── 加载训练好的 BiFlowNet checkpoint
   └── 设置疾病类别和生成参数

2. 生成每个患者
   ├── 对每个疾病类型 (RCM, ARVC)
   │   └── 对每个患者 ID
   │       ├── 生成 cine4ch latent → 解码 → 保存
   │       ├── 生成 cinesax latent → 解码 → 保存
   │       ├── 生成 lgesax latent → 解码 → 保存 (lge_sax_mid.nii.gz)
   │       └── 生成 lgesax2 latent → 解码 → 保存 (lge_sax_mid6.nii.gz)

3. 输出目录结构
   synthetic_data/
     synthetic_center_batch/
       RCM_niigz/
         generated_patient_id_001/
           0_final_custom_cine4ch_virtual/
             cine_4ch_mid.nii.gz
           0_final_custom_cinesax_virtual/
             cine_sax_mid.nii.gz
           0_final_custom_lgesax_virtual/
             lge_sax_mid.nii.gz
             lge_sax_mid6.nii.gz
```

### 4.2 生成参数

```yaml
generation:
  output_root: "/output/synthetic_data"
  target_diseases: ["RCM_niigz", "ARVC_niigz"]
  num_patients_per_disease: 3
  patient_id_prefix: "generated_patient_id"
  patient_id_start: 1
  center_name: "synthetic_center_batch"
  modalities_to_generate: ["cine4ch", "cinesax", "lgesax"]

  volume_sizes:
    cine4ch: [512, 512, 1]
    cinesax: [512, 512, 1]
    lgesax: [256, 256, 1]
    lgesax2: [256, 256, 1]

  sampling:
    strategy: "ddim"
    ddim_steps: 50
    eta: 0.0
```

---

## 5. 训练流程

### 5.1 训练步骤

```
Step 1: 数据准备
    └── 运行 verify_config.py 验证数据加载

Step 2: AutoEncoder Stage 1 训练
    └── python train/train_PatchVolume.py --config config/PatchVolume_8x.yaml

Step 3: AutoEncoder Stage 2 微调
    └── python train/train_PatchVolume_stage2.py --config config/PatchVolume_8x_s2.yaml

Step 4: 编码训练数据为 Latent
    └── python train/generate_training_latent.py --AE-ckpt <checkpoint>

Step 5: BiFlowNet 训练
    └── torchrun train/train_BiFlowNet_SingleRes.py --AE-ckpt <checkpoint>

Step 6: 生成合成数据
    └── python generate_synthetic_dataset.py --config configs/cardiac_pipeline.yaml
```

### 5.2 训练配置

| 阶段 | Batch Size | 学习率 | Epochs | GPU |
|------|------------|--------|--------|-----|
| AutoEncoder Stage 1 | 32 | 3e-4 | 10000 | H20-3e (140GB) |
| AutoEncoder Stage 2 | 8 | 3e-5 | 10000 | H20-3e (140GB) |
| BiFlowNet | 16 | 1e-4 | - | H20-3e (140GB) |

---

## 6. 关键设计决策

### 6.1 Wrapper 模式

BiFlowNetCardiac 包装原始 BiFlowNet，不改动原始代码：

```python
class BiFlowNetCardiac(nn.Module):
    def __init__(self, original_model, text_embed_dim=256):
        super().__init__()
        self.original_model = original_model
        # Text conditioning (future)
```

### 6.2 配置驱动

所有参数通过 YAML 配置文件管理：

```
configs/cardiac_pipeline.yaml    — 主配置
config/PatchVolume_8x.yaml       — AutoEncoder Stage 1
config/PatchVolume_8x_s2.yaml    — AutoEncoder Stage 2
```

### 6.3 目录镜像

输出严格复刻输入的 6 级目录结构：

```
根目录 / 中心批次 / 疾病类型 / 患者ID / 模态目录 / NIfTI文件
```

### 6.4 模态对齐

处理不同模态的维度差异：

- cinesax: 3 切片 → mid_only 策略 → 1 切片
- cine: 25 帧 → mid_only 策略 → 1 帧
- lgesax: 6 层 → mid_only 策略 → 1 层

---

## 7. 模型参数量

| 组件 | 参数量 | 说明 |
|------|--------|------|
| AutoEncoder Encoder | ~50M | 编码器 |
| AutoEncoder Decoder | ~50M | 解码器 |
| VQ Codebook | 8192 × 8 | 码本 |
| BiFlowNet | ~100M | 扩散模型 |
| **总计** | ~200M | |

---

## 8. 输出格式

### 8.1 NIfTI 文件格式

- 数据类型: float32
- 维度: 3D (H, W, D)
- 值范围: [-1, 1]（归一化后）
- 体素尺寸: 与输入数据一致

### 8.2 目录结构示例

```
synthetic_data/
└── synthetic_center_batch/
    ├── RCM_niigz/
    │   ├── generated_patient_id_001/
    │   │   ├── 0_final_custom_cine4ch_virtual/
    │   │   │   └── cine_4ch_mid.nii.gz
    │   │   ├── 0_final_custom_cinesax_virtual/
    │   │   │   └── cine_sax_mid.nii.gz
    │   │   └── 0_final_custom_lgesax_virtual/
    │   │       ├── lge_sax_mid.nii.gz
    │   │       └── lge_sax_mid6.nii.gz
    │   └── generated_patient_id_002/
    │       └── ...
    └── ARVC_niigz/
        └── ...
```

---

## 9. 评估指标

### 9.1 定量指标

- FID (Fréchet Inception Distance)
- SSIM (Structural Similarity Index)
- PSNR (Peak Signal-to-Noise Ratio)

### 9.2 定性评估

- 视觉质量评估
- 疾病特征保留
- 解剖结构合理性

---

## 10. 局限性与未来工作

### 10.1 当前局限

1. 文本-影像对齐未实现（知识库模块封存）
2. 仅支持单帧/单切片输出
3. 生成分辨率受限于训练 patch 大小

### 10.2 未来工作

1. 实现 CLIP-style 文本-影像对齐
2. 支持多帧/多切片生成
3. 提高生成分辨率
4. 添加更多疾病类型
