# 通用医学影像模型适配指南

> **目的：** 从 3D-MedDiffusion 项目经验中提炼的通用方法论，适用于任何医学影像生成/分割/分类模型的数据适配。
>
> **核心思想：** 80% 的报错来自 3 个根因——维度不整除、参数不一致、初始化顺序错。抓住这三点，绝大部分模型都能一次跑通。
>
> **最后更新：** 2026-05-13

---

## 目录

1. [适配前必读：三大铁律](#1-三大铁律)
2. [数据准备通用流程](#2-数据准备通用流程)
3. [模型分析检查清单](#3-模型分析检查清单)
4. [配置对齐方法](#4-配置对齐方法)
5. [小批量验证协议](#5-小批量验证协议)
6. [踩坑实录与通用教训](#6-踩坑实录与通用教训)
7. [适配报告模板](#7-适配报告模板)

---

## 1. 三大铁律

### 铁律一：维度必须整除

**任何**涉及 patch、sub-volume、下采样的模型，空间维度必须能被对应因子整除。

```
检查公式：data_dim % model_divisor == 0

常见 divisor：
  - patch_size        (AutoEncoder, ViT)
  - sub_volume_size   (BiFlowNet, 3D UNet)
  - downsample factor (U-Net 每层 ×2 或 ×4)
  - 注意力头数        (Transformer 的 num_heads)
```

**如果维度不整除 → 必须 padding**

```python
# 通用 padding 策略
padded_dim = ((data_dim // divisor) + 1) * divisor
# 例如：25 % 8 = 1 → padded = 32
```

**坑：padding 必须在所有分支路径之前统一完成**

```
错误做法：
  x → pad → branch_A
  x → branch_B (没 pad)  ← 维度不匹配！

正确做法：
  x → pad → branch_A
        → branch_B  ← 两条路径都在 padded 维度上工作
```

### 铁律二：参数必须跨文件一致

一个训练管线通常有 3-4 个地方定义同一个参数。必须保证全部一致。

| 参数类型 | 常见定义位置 | 不一致后果 |
|----------|-------------|-----------|
| latent 通道数 | config YAML, CLI args, 模型 __init__ | size mismatch |
| 空间分辨率 | config YAML, 数据 shape, 模型内部 | 维度报错 |
| 类别数 | config YAML, CLI args, embedding 层 | weight shape mismatch |
| 数据路径 | config YAML, dataset 类, 生成脚本 | FileNotFoundError |

**验证公式：**
```
image_size[0] = input_depth / downsample[0]
image_size[1] = input_height / downsample[1]
image_size[2] = input_width / downsample[2]
embedding_dim == volume_channels == in_channels
num_classes (YAML) == num_classes (CLI) == num_classes (模型结构)
```

### 铁律三：小批量验证先行

**绝对不要直接在完整数据集上训练。**

```
验证时间线：
  数据加载验证    → 1 分钟
  前向传播验证    → 2 分钟
  单 epoch 验证   → 5 分钟
  完整流程验证    → 10 分钟
  ─────────────────────
  总计            → ~20 分钟

vs 直接训练发现问题：
  排队等 GPU      → 30 分钟
  训跑到报错      → 2 小时
  调试修复        → 1 小时
  重新提交        → 30 分钟
  ─────────────────────
  总计            → 4+ 小时
```

---

## 2. 数据准备通用流程

### 2.1 数据格式检查

```python
import nibabel as nib  # NIfTI
import numpy as np

def check_data_format(file_path):
    """通用数据格式检查"""
    if file_path.endswith('.nii.gz'):
        img = nib.load(file_path)
        data = img.get_fdata()
        print(f"Shape: {data.shape}")
        print(f"Dtype: {data.dtype}")
        print(f"Range: [{data.min():.4f}, {data.max():.4f}]")
        print(f"Affine:\n{img.affine}")
        return data.shape
    elif file_path.endswith('.npy'):
        data = np.load(file_path)
        print(f"Shape: {data.shape}")
        print(f"Dtype: {data.dtype}")
        return data.shape
```

### 2.2 维度兼容性检查

```python
def check_dimension_compatibility(data_shape, model_config):
    """
    data_shape: (D, H, W) 或 (H, W, D) — 取决于模型要求
    model_config: dict with downsample, patch_size, etc.
    """
    issues = []
    dims = ['D', 'H', 'W']

    for i, (dim_name, data_dim) in enumerate(zip(dims, data_shape)):
        divisor = model_config['downsample'][i]
        if data_dim % divisor != 0:
            padded = ((data_dim // divisor) + 1) * divisor
            issues.append({
                'dim': dim_name,
                'current': data_dim,
                'divisor': divisor,
                'remainder': data_dim % divisor,
                'padded_to': padded
            })

    if issues:
        print("⚠️ 维度兼容性问题：")
        for issue in issues:
            print(f"  {issue['dim']}: {issue['current']} % {issue['divisor']} = {issue['remainder']}")
            print(f"    → 需要 padding 到 {issue['padded_to']}")
    else:
        print("✅ 所有维度兼容")

    return issues
```

### 2.3 数据目录结构验证

```python
import os
from pathlib import Path

def verify_dataset_structure(root_dir, expected_structure):
    """
    expected_structure: dict 定义期望的目录结构
    例如: {
        'required_dirs': ['patient_id/0_final_custom_cine4ch'],
        'required_files': ['cine_4ch_mid.nii.gz'],
        'min_patients': 10
    }
    """
    errors = []

    # 检查根目录
    if not os.path.exists(root_dir):
        errors.append(f"根目录不存在: {root_dir}")
        return errors

    # 检查患者数量
    patients = [d for d in os.listdir(root_dir)
                if os.path.isdir(os.path.join(root_dir, d))]
    if len(patients) < expected_structure.get('min_patients', 1):
        errors.append(f"患者数量不足: {len(patients)} < {expected_structure['min_patients']}")

    # 检查必需文件
    for patient in patients[:3]:  # 只检查前 3 个
        patient_path = os.path.join(root_dir, patient)
        for required_file in expected_structure.get('required_files', []):
            file_path = os.path.join(patient_path, required_file)
            if not os.path.exists(file_path):
                errors.append(f"缺失文件: {patient}/{required_file}")

    return errors
```

---

## 3. 模型分析检查清单

拿到一个新模型，按以下顺序分析：

### 3.1 必须找到的信息

| 信息 | 在哪里找 | 为什么重要 |
|------|----------|-----------|
| `image_size` / `input_shape` | config YAML / 模型 __init__ | 决定输入尺寸 |
| `downsample` / `patch_size` | config YAML / 模型代码 | 决定维度整除要求 |
| `num_classes` | config YAML / embedding 层 | 决定条件化维度 |
| `embedding_dim` / `in_channels` | config YAML / 模型代码 | 决定 latent 通道数 |
| 参数加载顺序 | 训练脚本 | 防止初始化顺序陷阱 |

### 3.2 参数加载顺序检查

```python
# 在训练脚本中搜索以下模式：
# 1. 模型初始化位置
model = SomeModel(
    num_classes=args.num_classes,  # ← 此时 args.num_classes 是什么值？
    ...
)

# 2. 参数更新位置
args.num_classes = config['data']['num_classes']  # ← 如果在初始化之后更新，就晚了！

# 正确顺序应该是：
# 先更新 args，再初始化 model
```

### 3.3 输出分辨率计算

```python
def calculate_output_resolution(model_config):
    """根据模型配置计算最大输出分辨率"""
    image_size = model_config['image_size']  # latent 空间尺寸
    downsample = model_config['downsample']  # 降采样因子

    output = []
    for latent_dim, ds_factor in zip(image_size, downsample):
        output.append(latent_dim * ds_factor)

    # 注意：Conv3d 可能 +1 像素
    output_with_padding = [d + 1 if ds > 1 else d
                           for d, ds in zip(output, downsample)]

    print(f"理论输出: {output}")
    print(f"实际输出（含 Conv3d padding）: {output_with_padding}")
    return output
```

---

## 4. 配置对齐方法

### 4.1 创建参数追踪表

```yaml
# parameter_tracking.yaml — 手动维护，确保一致性
parameters:
  embedding_dim:
    yaml_value: 8
    cli_flag: "--volume-channels"
    cli_value: 8
    model_value: 8  # 从模型代码中读取
    status: "✅ 一致"

  num_classes:
    yaml_value: 5
    cli_flag: "--num-classes"
    cli_value: 5
    model_value: 7  # ⚠️ 模型用默认值初始化
    status: "⚠️ 不一致！模型结构是 7，配置是 5"

  image_size:
    yaml_value: [25, 16, 16]
    data_shape: [256, 256, 25]  # 原始数据
    downsample: [1, 8, 8]
    expected_latent: [25, 32, 32]  # 256/8=32
    actual_latent: [25, 16, 16]  # ⚠️ 不匹配
    status: "⚠️ image_size 与数据计算不一致"
```

### 4.2 自动化验证脚本

```python
import yaml

def validate_config_consistency(config_path):
    """验证配置文件内部一致性"""
    with open(config_path) as f:
        config = yaml.safe_load(f)

    errors = []

    # 检查每个模态
    for modality in ['cine', 'lge']:
        if modality not in config.get('model', {}):
            continue

        ae = config['model'][modality]['autoencoder']
        diff = config['model'][modality]['diffusion']

        # 检查 latent 尺寸
        expected_d = ae['patch_depth'] // ae['downsample'][0]
        expected_h = ae['patch_size'] // ae['downsample'][1]
        expected_w = ae['patch_size'] // ae['downsample'][2]

        if diff['image_size'] != [expected_d, expected_h, expected_w]:
            errors.append(
                f"{modality}: image_size {diff['image_size']} != "
                f"计算值 [{expected_d}, {expected_h}, {expected_w}]"
            )

        # 检查通道数
        if ae['embedding_dim'] != diff.get('in_channels', ae['embedding_dim']):
            errors.append(
                f"{modality}: embedding_dim {ae['embedding_dim']} != "
                f"in_channels {diff['in_channels']}"
            )

    if errors:
        print("❌ 配置不一致：")
        for e in errors:
            print(f"  - {e}")
    else:
        print("✅ 配置一致")

    return errors
```

---

## 5. 小批量验证协议

### 5.1 构建 mini dataset

```python
import numpy as np
import nibabel as nib
import os

def create_mini_dataset(source_dir, target_dir, num_patients=2):
    """
    从真实数据中抽取少量样本构建测试数据集
    保持目录结构完全一致
    """
    patients = sorted(os.listdir(source_dir))[:num_patients]

    for patient in patients:
        src = os.path.join(source_dir, patient)
        dst = os.path.join(target_dir, patient)

        # 复制目录结构（只复制前几个文件）
        for root, dirs, files in os.walk(src):
            rel_path = os.path.relpath(root, src)
            dst_path = os.path.join(dst, rel_path)
            os.makedirs(dst_path, exist_ok=True)

            for f in files[:2]:  # 每个目录只复制 2 个文件
                src_file = os.path.join(root, f)
                dst_file = os.path.join(dst_path, f)
                # 复制或创建符号链接
                os.symlink(src_file, dst_file)

    print(f"✅ Mini dataset 创建完成: {target_dir}")
    print(f"   患者数: {num_patients}")
```

### 5.2 验证检查点

```yaml
# mini_validation_checklist.yaml
stages:
  - name: "数据加载"
    checks:
      - "所有文件可以正常读取"
      - "数据 shape 与预期一致"
      - "数据范围合理（无 NaN/Inf）"
    pass_criteria: "无报错，shape 正确"

  - name: "前向传播"
    checks:
      - "模型可以接收数据"
      - "输出 shape 正确"
      - "无维度不匹配错误"
    pass_criteria: "无 RuntimeError"

  - name: "单 epoch 训练"
    checks:
      - "Loss 有值（非 NaN）"
      - "Loss 有下降趋势"
      - "显存使用合理"
    pass_criteria: "Loss 从 ~1.0 下降到 ~0.5"

  - name: "推理/生成"
    checks:
      - "可以生成输出"
      - "输出 shape 正确"
      - "输出值范围合理"
    pass_criteria: "输出文件可以正常打开"
```

---

## 6. 踩坑实录与通用教训

### 坑 1：维度不整除导致 shape mismatch

**3D-MedDiffusion 实例：** Latent D=25 不能被 sub_volume_size=8 整除，导致 BiFlowNet forward 中多处维度不匹配。

**通用教训：**
- 拿到模型第一件事：检查所有空间维度是否能被对应的 divisor 整除
- 如果不整除，必须在 forward 最开始统一 padding，不要在中间路径单独 pad

**检测方法：**
```bash
# 搜索代码中的 divisor
grep -r "sub_volume_size\|patch_size\|downsample" --include="*.py" --include="*.yaml"
```

### 坑 2：参数跨文件不一致

**3D-MedDiffusion 实例：** `num_classes` 在 YAML 中定义为 5，但模型初始化时用默认值 7。

**通用教训：**
- 同一个参数可能在 config YAML、CLI args、模型 __init__、dataset 类中分别定义
- 必须逐一检查并确认一致
- 特别注意模型初始化和参数加载的顺序

**检测方法：**
```bash
# 搜索参数在各处的定义
grep -r "num_classes\|embedding_dim\|image_size" --include="*.py" --include="*.yaml"
```

### 坑 3：路径拼接错误

**3D-MedDiffusion 实例：** Dataset 类自动添加 `_latents` 后缀，导致路径变成 `.../cine_latent_latents`。

**通用教训：**
- Dataset 类的路径拼接逻辑可能与你预期不同
- 打印实际拼接后的路径进行验证
- 注意相对路径 vs 绝对路径

**检测方法：**
```python
# 在 dataset 类中添加调试输出
print(f"实际加载路径: {full_path}")
```

### 坑 4：Conv3d 输出 +1 像素

**3D-MedDiffusion 实例：** 期望输出 128×128，实际 129×129。

**通用教训：**
- Conv3d 的 padding 行为可能导致输出比预期多 1 像素
- 这通常是正常行为，不是 bug
- 如果严格要求尺寸，需要在输出后裁剪

### 坑 5：checkpoint 恢复创建新目录

**3D-MedDiffusion 实例：** 不带 `--ckpt` 启动会创建新目录（编号+1），导致多次报错重启产生多个空目录。

**通用教训：**
- 恢复训练务必使用显式的 checkpoint 参数
- 启动前检查是否已有同名实验目录

### 坑 6：输出分辨率由训练配置决定

**3D-MedDiffusion 实例：** 训练时 `image_size=[25,16,16]`，最大输出 128×128。想输出 256×256 必须重新训练。

**通用教训：**
- 不要假设模型能输出任意分辨率
- 输出分辨率 = `latent_size × upsample_factor`
- 要改变输出分辨率，必须重新训练

---

## 7. 适配报告模板

每次适配新模型时，生成以下报告：

```markdown
# 模型适配报告

## 基本信息
- 模型名称：
- 数据格式：
- 数据维度：
- 目标分辨率：

## 维度兼容性
| 维度 | 数据 | Divisor | 余数 | 状态 |
|------|------|---------|------|------|
| D | | | | ✅/⚠️ |
| H | | | | ✅/⚠️ |
| W | | | | ✅/⚠️ |

## 参数一致性
| 参数 | YAML | CLI | 模型结构 | 状态 |
|------|------|-----|----------|------|
| embedding_dim | | | | ✅/⚠️ |
| num_classes | | | | ✅/⚠️ |
| image_size | | | | ✅/⚠️ |

## 已知坑
- [ ] 坑1: 维度整除性
- [ ] 坑2: 参数一致性
- [ ] 坑3: 路径拼接
- [ ] 坑4: Conv3d +1
- [ ] 坑5: checkpoint 恢复
- [ ] 坑6: 输出分辨率

## 验证结果
- [ ] 数据加载通过
- [ ] 前向传播通过
- [ ] 单 epoch 训练通过
- [ ] 推理生成通过

## 预期输出
- 分辨率：
- 文件格式：
- 文件数量：
```

---

## 附录：快速命令参考

```bash
# 1. 检查数据 shape
python -c "import nibabel as nib; img = nib.load('data.nii.gz'); print(img.shape)"

# 2. 检查模型配置
grep -r "image_size\|num_classes\|embedding_dim" --include="*.yaml" configs/

# 3. 检查维度整除性
python -c "
data_dim = 25
divisor = 8
print(f'{data_dim} % {divisor} = {data_dim % divisor}')
print(f'需要 padding 到 {((data_dim // divisor) + 1) * divisor}')
"

# 4. 搜索参数在代码中的使用
grep -rn "num_classes\|args\.num_classes" --include="*.py" train/

# 5. 检查 checkpoint 文件
ls -la checkpoints/*.ckpt checkpoints/*.pt
```

---

**文档来源：** 3D-MedDiffusion 项目实战经验
**适用范围：** 任何 3D 医学影像生成/分割/分类模型
**维护建议：** 每次遇到新坑，更新第 6 节"踩坑实录"
