# 3D-MedDiffusion 项目结构说明

## 项目概述

本项目基于 3D-MedDiffusion 构建，用于生成心肌病（RCM、ARVC、DCM、HCM、LVNC）的合成心脏 MRI 数据。

---

## 目录结构

```
3D-MedDiffusion/
├── configs/                          # 主配置文件目录
│   └── cardiac_pipeline.yaml         # 主配置文件（数据路径、模型参数、训练参数）
│
├── config/                           # AutoEncoder训练配置
│   ├── PatchVolume_8x.yaml           # AutoEncoder Stage 1 配置
│   ├── PatchVolume_8x_s2.yaml        # AutoEncoder Stage 2 配置
│   ├── PatchVolume_data.json         # 数据路径配置
│   ├── SingleRes_dataset.json        # Latent编码配置
│   └── base_cfg.yaml                 # 基础配置
│
├── data_loaders/                     # 数据加载模块
│   ├── __init__.py
│   └── cardiac_dataset.py            # 心脏MRI数据集类（支持分模态）
│
├── AutoEncoder/                      # 自编码器模块
│   ├── model/
│   │   ├── PatchVolume.py            # PatchVolume模型
│   │   ├── MedicalNetPerceptual.py   # MedicalNet感知损失
│   │   ├── codebook.py               # VQ码本
│   │   ├── lpips.py                  # LPIPS感知损失
│   │   └── __init__.py
│   └── utils.py
│
├── ddpm/                             # 扩散模型模块
│   ├── BiFlowNet.py                  # BiFlowNet基础模型
│   ├── BiFlowNet_cardiac.py          # BiFlowNet心脏版本（Wrapper）
│   ├── utils.py
│   └── __init__.py
│
├── dataset/                          # 数据集工具
│   ├── Singleres_dataset.py          # 单分辨率数据集
│   ├── tr_generate.py                # 训练数据生成
│   ├── vqgan.py                      # VQ-GAN数据集
│   ├── vqgan_4x.py                   # VQ-GAN 4x数据集
│   └── __init__.py
│
├── train/                            # 训练脚本
│   ├── train_PatchVolume.py          # AutoEncoder Stage 1 训练
│   ├── train_PatchVolume_stage2.py   # AutoEncoder Stage 2 训练
│   ├── train_BiFlowNet_SingleRes.py  # BiFlowNet训练
│   ├── generate_training_latent.py   # 生成训练latent
│   └── callbacks.py                  # 训练回调
│
├── evaluation/                       # 评估脚本
│   ├── class_conditional_generation.py
│   └── class_conditional_generation_4x.py
│
├── knowledge_base/                   # 知识库模块（当前封存）
│   ├── __init__.py
│   ├── pdf_parser.py
│   └── create_placeholder_pdfs.py
│
├── warvito_MedicalNet-models_main/   # MedicalNet预训练模型
│   ├── hubconf.py
│   └── medicalnet_models/
│
├── docs/                             # 原始项目文档网站
├── assets/                           # 原始项目图片资源
│
├── check_single_patient.py           # 检查单个患者数据尺寸
├── verify_config.py                  # 验证配置与数据匹配
├── generate_synthetic_dataset.py     # 生成合成数据集
│
├── 操作手册.md                        # 详细操作手册
├── 模型架构说明.md                    # 模型架构说明
├── 当前缺陷与改进方案.md              # 缺陷分析与改进方案
├── 项目结构说明.md                    # 本文件
│
├── 活动日志/                          # 活动日志目录
│   └── 2026-05-07.md
│
├── logs/                             # 训练日志目录
├── synthetic_output/                 # 合成数据输出目录
├── requirements.txt                  # Python依赖
├── 3DMedDiffusion.yaml               # Conda环境配置
├── README.md                         # 原始项目README
└── .gitignore                        # Git忽略规则
```

---

## 核心文件说明

### 配置文件

| 文件 | 用途 | 关键参数 |
|------|------|----------|
| `configs/cardiac_pipeline.yaml` | 主配置文件 | 数据路径、模型参数、训练参数、生成参数 |
| `config/PatchVolume_8x.yaml` | AutoEncoder Stage 1 | batch_size=32, learning_rate=3e-4 |
| `config/PatchVolume_8x_s2.yaml` | AutoEncoder Stage 2 | batch_size=8, learning_rate=3e-5 |

### 核心代码

| 文件 | 用途 |
|------|------|
| `data_loaders/cardiac_dataset.py` | 数据加载，支持分模态（cine/lge） |
| `AutoEncoder/model/PatchVolume.py` | VQ-GAN自编码器 |
| `ddpm/BiFlowNet.py` | 扩散模型 |
| `ddpm/BiFlowNet_cardiac.py` | 心脏版本Wrapper |
| `train/train_PatchVolume.py` | AutoEncoder训练 |
| `train/train_BiFlowNet_SingleRes.py` | BiFlowNet训练 |
| `generate_synthetic_dataset.py` | 合成数据生成 |

### 工具脚本

| 文件 | 用途 |
|------|------|
| `verify_config.py` | 验证配置与数据匹配 |
| `check_single_patient.py` | 检查单个患者数据尺寸 |

---

## 文档说明

| 文件 | 内容 |
|------|------|
| `操作手册.md` | 详细操作步骤、服务器环境、配置参数详解 |
| `模型架构说明.md` | 模型架构、数据格式、训练流程 |
| `当前缺陷与改进方案.md` | 已知缺陷、改进方案、版本规划 |
| `活动日志/2026-05-07.md` | 2026-05-07的工作记录 |

---

## 分模态训练架构

### 模型分离

| 模型 | 处理模态 | D维度 | batch_size | latent_size |
|------|----------|-------|------------|-------------|
| Cine模型 | cine4ch + cinesax | 25 | 4 | [16, 16, 25] |
| LGE模型 | lgesax | 6 | 16 | [16, 16, 6] |

### 数据流

```
输入数据:
  cine4ch: (256, 256, 25) → Cine模型 → (128, 128, 25) patch
  cinesax: (256, 256, 25) × 3 → Cine模型 → (128, 128, 25) patch
  lgesax:  (256, 256, 6)  → LGE模型 → (128, 128, 6) patch

输出数据:
  每个患者生成10个文件:
  - 1个cine4ch文件
  - 3个cinesax文件 (down, mid, up)
  - 6个lgesax文件 (mid1-6)
```

---

## 快速开始

### 1. 环境配置

```bash
conda activate 3DMedDiffusion
export CUDA_VISIBLE_DEVICES=0
```

### 2. 验证配置

```bash
python verify_config.py --cine
python verify_config.py --lge
```

### 3. 训练流程

```bash
# Cine模型
python train/train_PatchVolume.py --config config/PatchVolume_8x.yaml --modality cine
python train/train_PatchVolume_stage2.py --config config/PatchVolume_8x_s2.yaml --modality cine

# LGE模型
python train/train_PatchVolume.py --config config/PatchVolume_8x.yaml --modality lge
python train/train_PatchVolume_stage2.py --config config/PatchVolume_8x_s2.yaml --modality lge
```

### 4. 生成数据

```bash
python generate_synthetic_dataset.py --config configs/cardiac_pipeline.yaml
```

---

## 已知限制

1. **分模态训练导致病人级关联缺失** - 见 `当前缺陷与改进方案.md`
2. **帧选择策略简单** - 当前只取中间帧
3. **缺乏训练监控** - 未集成TensorBoard
4. **缺乏自动化质量评估** - 未实现FID/SSIM计算

---

## 后续改进

详见 `当前缺陷与改进方案.md`

**优先级：**
1. P0: 解决病人级关联问题（跨模态条件化或统一模型）
2. P1: 添加训练监控和质量评估
3. P2: 优化帧选择和数据增强

---

## 联系方式

如有问题，请参考：
- `操作手册.md` - 详细操作步骤
- `模型架构说明.md` - 技术细节
- `活动日志/` - 历史记录

---

**最后更新：** 2026-05-07
