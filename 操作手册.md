# 3D-MedDiffusion 心肌病数据生成 — 操作手册

## 目录

1. [项目概述](#1-项目概述)
2. [服务器环境](#2-服务器环境)
3. [配置参数详解](#3-配置参数详解)
4. [正式运行流程](#4-正式运行流程)
5. [常见问题](#5-常见问题)

---

## 1. 项目概述

本项目基于 [3D-MedDiffusion](https://github.com/oneseafor/3D-MedDiffusion) 构建，
目标是为罕见心肌病（RCM、ARVC 等）生成高质量的 3D 心脏 MRI 合成数据。

**数据格式：** 6 级目录结构

```
根目录 / 中心批次 / 疾病类型 / 患者ID / 模态目录 / NIfTI文件
```

**支持的模态：**
- `cine4ch` — 长轴电影 MRI（25帧，输出1个文件）
- `cinesax` — 短轴电影 MRI（3切片×25帧，输出1个文件）
- `lgesax` — 延迟增强 MRI（6层切片，输出2个文件）

**输出文件说明：**

| 模态 | 输入尺寸 | 输出尺寸 | 输出文件数 |
|------|----------|----------|------------|
| cine4ch | (512, 512, 25) | (512, 512, 1) | 1 |
| cinesax | (512, 512, 25) × 3 | (512, 512, 1) | 1 |
| lgesax | (256, 256, 6) | (256, 256, 1) | 2 |

**LGE 输出文件名：**
- `lge_sax_mid.nii.gz` — 第一个 LGE
- `lge_sax_mid6.nii.gz` — 第二个 LGE

**支持的疾病：** ARVC、RCM、DCM、HCM、LVNC

---

## 2. 服务器环境

### 2.1 环境信息

| 项目 | 值 |
|------|-----|
| 环境名称 | `3DMedDiffusion` |
| PyTorch | 2.2.0+cu121 (CUDA 12.1) |
| MONAI | 1.4.0 |
| Python | 3.11 |
| GPU | NVIDIA H20-3e (140GB 显存) × 8 |
| 使用 GPU | 第一张卡（编号 0） |

### 2.2 激活环境与设置 GPU

```bash
conda activate 3DMedDiffusion
export CUDA_VISIBLE_DEVICES=0
```

### 2.3 验证 CUDA

```bash
python -c "import torch; print(f'CUDA: {torch.cuda.is_available()}, GPU: {torch.cuda.get_device_name(0) if torch.cuda.is_available() else \"N/A\"}')"
```

### 2.4 创建输出目录

```bash
mkdir -p /home/kangchang.shen/out_put/3D-MedDiffusion/{synthetic_data,logs,checkpoints/{patchvolume_stage1,patchvolume_stage2}}
```

---

## 3. 配置参数详解

### ⭐ 3.1 生成数据参数（重点）

**文件位置：** `configs/cardiac_pipeline.yaml`

**如果你想多生成数据，修改以下参数：**

```yaml
# --- Generation / Export ---
generation:
  output_root: "/home/kangchang.shen/out_put/3D-MedDiffusion/synthetic_data"

  # ⭐ 修改这里：要生成哪些疾病类型
  target_diseases: ["RCM_niigz", "ARVC_niigz"]

  # ⭐ 修改这里：每种疾病生成多少患者（当前3个，可增大）
  num_patients_per_disease: 3

  # ⭐ 修改这里：患者ID前缀和起始编号
  patient_id_prefix: "generated_patient_id"
  patient_id_start: 1

  # ⭐ 修改这里：输出的中心批次名称
  center_name: "synthetic_center_batch"

  # ⭐ 修改这里：要生成哪些模态
  modalities_to_generate:
    - "cine4ch"
    - "cinesax"
    - "lgesax"

  # ⭐ 修改这里：输出体积尺寸（已根据实际数据配置）
  # cine4ch: 512×512 单帧
  # cinesax: 512×512 单帧 (mid slice)
  # lgesax: 256×256 单切片
  volume_sizes:
    cine4ch: [512, 512, 1]
    cinesax: [512, 512, 1]
    lgesax:  [256, 256, 1]

  # ⭐ 修改这里：采样参数
  sampling:
    strategy: "ddim"       # 采样策略：ddim 或 ddpm
    ddim_steps: 50         # DDIM采样步数（越快质量越低）
    eta: 0.0               # 噪声系数（0=确定性，1=随机）
```

**常见修改场景：**

| 场景 | 修改内容 |
|------|----------|
| 生成更多患者 | 增大 `num_patients_per_disease`（如改为 10、50） |
| 生成更多疾病类型 | 在 `target_diseases` 添加疾病（如 `DCM_niigz`、`HCM_niigz`） |
| 生成更高质量 | 增大 `ddim_steps`（如 100、200） |
| 加快生成速度 | 减小 `ddim_steps`（如 20） |
| 调整输出尺寸 | 修改 `volume_sizes`（需与输入数据一致） |

---

### 3.2 数据源配置

**文件位置：** `configs/cardiac_pipeline.yaml`

```yaml
# --- Data Source ---
data:
  # 数据根目录（6级结构的根）
  root_dir: "/home/zirui/yuansq/CMR-AI/0_data_CMRAI"

  # 模态目录映射（通常不改）
  modality_mapping:
    cine4ch: "0_final_custom_cine4ch"
    cinesax: "0_final_custom_cinesax"
    lgesax:  "0_final_custom_lgesax"

  # 疾病类别（训练时包含哪些疾病）
  disease_categories:
    - "ARVC_niigz"
    - "RCM_niigz"
    - "DCM_niigz"
    - "HCM_niigz"
    - "LVNC_niigz"

  # NIfTI文件命名（根据实际文件名修改）
  file_naming:
    cine4ch: ["cine_4ch_mid.nii.gz"]
    cinesax: ["cine_sax_down.nii.gz", "cine_sax_mid.nii.gz", "cine_sax_up.nii.gz"]
    lgesax:  ["lge_sax_mid6.nii.gz"]

  # cinesax切片对齐策略（3个切片如何处理）
  slice_alignment:
    strategy: "mid_only"  # 选项: mid_only, interpolate, stack

  # ⭐ 电影序列帧选择（cine数据有25帧）
  frame_selection:
    strategy: "mid_only"  # 选项: mid_only, average, stack, first, last
    frame_index: 12       # 当 strategy 为 mid_only/first/last 时使用

  # ⭐ lge切片选择（lge数据有6层）
  lge_slice_selection:
    strategy: "mid_only"  # 选项: mid_only, average, stack, specific
    slice_index: 3        # 当 strategy 为 mid_only/specific 时使用

  # 输入维度（经过帧/切片选择后）
  input_dimensions:
    cine4ch: [512, 512, 1]  # 单帧
    cinesax: [512, 512, 1]  # 单帧 (mid slice)
    lgesax:  [256, 256, 1]  # 单切片
```

**数据处理流程：**

```
原始数据:
  cine4ch: (512, 512, 25)  → frame_selection → (512, 512, 1)
  cinesax: (512, 512, 25) × 3切片 → slice_alignment → frame_selection → (512, 512, 1)
  lgesax:  (256, 256, 6)  → lge_slice_selection → (256, 256, 1)
```

**帧/切片选择策略说明：**

| 策略 | 说明 | 适用场景 |
|------|------|----------|
| `mid_only` | 取中间帧/切片 | 默认，推荐 |
| `average` | 平均所有帧/切片 | 平滑处理 |
| `stack` | 作为通道堆叠 | 需要修改模型输入通道 |
| `first` / `last` | 取首/末帧 | 特定需求 |
| `specific` | 取指定索引 | 自定义选择 |

---

### 3.3 模型架构参数

**文件位置：** `configs/cardiac_pipeline.yaml`

```yaml
# --- Model Architecture ---
model:
  autoencoder:
    downsample: [8, 8, 8]      # 下采样倍数
    in_channels: 1              # 输入通道数
    out_channels: 1             # 输出通道数
    n_hiddens: 48               # 隐藏层通道数
    embedding_dim: 8            # 嵌入维度
    n_codes: 8192               # 码本大小
    patch_size: 128             # ⭐ 训练时随机裁剪的 patch 大小
    n_groups: 24                # 分组归一化组数

  diffusion:
    image_size: [16, 16, 16]   # ⭐ latent空间尺寸 = patch_size / 8
    in_channels: 8              # 必须等于 embedding_dim
    model_channels: 128         # 模型通道数
    out_channels: 8             # 输出通道数
    num_res_blocks: 2           # 残差块数量
    attention_resolutions: [4, 2, 1]  # 注意力层分辨率
    num_heads: 4                # 注意力头数
    num_classes: 2              # 分类数（RCM和ARVC）
    class_names: ["RCM", "ARVC"]  # 类别名称
```

**当前数据尺寸与配置：**

| 模态 | 原始尺寸 | 处理后尺寸 | 输出尺寸 |
|------|----------|------------|----------|
| cine4ch | (512, 512, 25) | (512, 512, 1) | [512, 512, 1] |
| cinesax | (512, 512, 25) × 3 | (512, 512, 1) | [512, 512, 1] |
| lgesax | (256, 256, 6) | (256, 256, 1) | [256, 256, 1] |

**patch_size 选择说明：**

| 数据尺寸 | 建议 patch_size | 说明 |
|----------|-----------------|------|
| 256×256×N | 128 | 训练时随机裁剪，覆盖约50%区域 |
| 512×512×N | 128 或 256 | 128=快速训练，256=更多上下文 |

**当前配置：patch_size = 128**
- 对于 512×512 数据：训练时随机裁剪 128×128 区域
- 对于 256×256 数据：训练时随机裁剪 128×128 区域
- 生成时输出完整尺寸（512×512 或 256×256）

**latent 空间计算：**
```
latent_size = patch_size / downsample = 128 / 8 = 16
```
所以 `diffusion.image_size` = `[16, 16, 16]`

**📋 配置同步检查清单：**

| 参数 | 位置 | 当前值 | 说明 |
|------|------|--------|------|
| `volume_sizes.cine4ch` | `configs/cardiac_pipeline.yaml` | [512, 512, 1] | 输出尺寸 |
| `volume_sizes.cinesax` | `configs/cardiac_pipeline.yaml` | [512, 512, 1] | 输出尺寸 |
| `volume_sizes.lgesax` | `configs/cardiac_pipeline.yaml` | [256, 256, 1] | 输出尺寸 |
| `patch_size` | `configs/cardiac_pipeline.yaml` | 128 | 训练 patch |
| `image_size` | `configs/cardiac_pipeline.yaml` | [16, 16, 16] | latent 尺寸 |
| `patch_size` | `config/PatchVolume_8x.yaml` | 128 | AE Stage1 |
| `patch_size` | `config/PatchVolume_8x_s2.yaml` | 128 | AE Stage2 |

**⚠️ 修改 patch_size 时需同步修改：**

如果改为 patch_size = 256：
```yaml
# configs/cardiac_pipeline.yaml
model:
  autoencoder:
    patch_size: 256
  diffusion:
    image_size: [32, 32, 32]  # 256 / 8 = 32

# config/PatchVolume_8x.yaml & PatchVolume_8x_s2.yaml
dataset:
  patch_size: 256
```

---

### 3.4 训练参数

**文件位置：** `configs/cardiac_pipeline.yaml`

```yaml
# --- Training ---
training:
  batch_size: 16               # 批次大小（H20-3e 140GB可用较大值）
  learning_rate: 1.0e-4        # 学习率
  num_epochs: 100              # 训练轮数
  num_workers: 4               # 数据加载线程数
  precision: "16"              # 混合精度训练
  gradient_clip_val: 1.0       # 梯度裁剪

  autoencoder:
    stage1_epochs: 50          # Stage1训练轮数
    stage2_epochs: 20          # Stage2训练轮数
    disc_start: 5000           # 判别器启动步数
    perceptual_weight: 2.0     # 感知损失权重
    l1_weight: 4.0             # L1损失权重
    gan_weight: 2.0            # GAN损失权重

  diffusion:
    timesteps: 1000            # 扩散步数
    beta_schedule: "cosine"    # beta调度策略
    loss_type: "l2"            # 损失类型
    ema_decay: 0.995           # EMA衰减率
```

---

### 3.5 AutoEncoder 训练配置

**文件位置：** `config/PatchVolume_8x.yaml`（Stage 1）

```yaml
model:
  seed: 1234
  batch_size: 32               # ⭐ 可调整
  num_workers: 8
  gpus: 1
  default_root_dir: /home/kangchang.shen/out_put/3D-MedDiffusion/checkpoints/patchvolume_stage1
  max_epochs: 10000
  precision: 16                # 混合精度
  lr: 3e-4                     # 学习率
  downsample: [8, 8, 8]
  embedding_dim: 8
  n_codes: 8192
  n_hiddens: 48
  patch_size: 64

dataset:
  root_dir: 3DMedDiffusion/config/PatchVolume_data.json
  image_channels: 1
  imgtype: CT
  patch_size: 64
```

**文件位置：** `config/PatchVolume_8x_s2.yaml`（Stage 2）

```yaml
model:
  seed: 1234
  batch_size: 8                # ⭐ 可调整
  num_workers: 8
  gpus: 1
  default_root_dir: /home/kangchang.shen/out_put/3D-MedDiffusion/checkpoints/patchvolume_stage2
  resume_from_checkpoint: /home/kangchang.shen/out_put/3D-MedDiffusion/checkpoints/patchvolume_stage1/last.ckpt
  max_epochs: 10000
  precision: 16
  lr: 3e-5                     # Stage2用更小学习率
  stage: 2

dataset:
  root_dir: 3DMedDiffusion/config/PatchVolume_data.json
  image_channels: 1
  imgtype: CT
  patch_size: 64
```

---

### 3.6 日志配置

**文件位置：** `configs/cardiac_pipeline.yaml`

```yaml
# --- Logging ---
logging:
  log_dir: "/home/kangchang.shen/out_put/3D-MedDiffusion/logs"
  save_checkpoints: true
  checkpoint_dir: "/home/kangchang.shen/out_put/3D-MedDiffusion/checkpoints"
  log_interval: 10
```

---

## 4. 正式运行流程

### 4.1 验证配置与数据加载

```bash
conda activate 3DMedDiffusion
export CUDA_VISIBLE_DEVICES=0
cd /path/to/3D-MedDiffusion

# 运行配置验证脚本
python verify_config.py
```

**验证脚本会检查：**
1. 配置参数是否正确加载
2. Dataset 是否能创建成功
3. 数据能否正确加载
4. 帧/切片选择是否正确
5. 各模态尺寸是否与配置匹配
6. patch_size 是否合适

**预期输出示例：**
```
✓ Dataset 创建成功
  患者数量: 100

✓ 样本加载成功
  患者ID: complete_GE_155884_...
  疾病: ARVC_niigz
  模态数量: 3

【加载后的模态尺寸】
  vol_cine4ch: torch.Size([512, 512, 1])
  vol_cinesax: torch.Size([512, 512, 1])
  vol_lgesax: torch.Size([256, 256, 1])

【训练 patch 尺寸】
  volume: torch.Size([128, 128, 1])

✓ 所有模态尺寸与配置匹配
```

### 4.2 训练 AutoEncoder Stage 1

```bash
python train/train_PatchVolume.py --config config/PatchVolume_8x.yaml
```

### 4.3 训练 AutoEncoder Stage 2

```bash
python train/train_PatchVolume_stage2.py --config config/PatchVolume_8x_s2.yaml
```

### 4.4 编码训练数据为 latent

```bash
python train/generate_training_latent.py \
    --data-path config/Singleres_dataset.json \
    --AE-ckpt /home/kangchang.shen/out_put/3D-MedDiffusion/checkpoints/patchvolume_stage2/last.ckpt \
    --batch-size 8
```

### 4.5 训练 BiFlowNet（扩散模型）

```bash
CUDA_VISIBLE_DEVICES=0 torchrun --nnodes=1 --nproc_per_node=1 --master_port 29513 \
    train/train_BiFlowNet_SingleRes.py \
    --data-path config/Singleres_dataset.json \
    --results-dir /home/kangchang.shen/out_put/3D-MedDiffusion/checkpoints/ \
    --num-classes 5 \
    --AE-ckpt /home/kangchang.shen/out_put/3D-MedDiffusion/checkpoints/patchvolume_stage2/last.ckpt \
    --resolution 32 32 32 \
    --batch-size 16 \
    --num-workers 4
```

### 4.6 生成合成数据

```bash
python generate_synthetic_dataset.py --config configs/cardiac_pipeline.yaml
```

**输出位置：** `/home/kangchang.shen/out_put/3D-MedDiffusion/synthetic_data/`

### 4.7 验证输出

```bash
python generate_synthetic_dataset.py --config configs/cardiac_pipeline.yaml --verify-only
```

---

## 5. 常见问题

### Q: 如何生成更多数据？

修改 `configs/cardiac_pipeline.yaml` 中的生成参数：

```yaml
generation:
  # 生成更多患者
  num_patients_per_disease: 10   # 改大这个数字

  # 添加更多疾病类型
  target_diseases: ["RCM_niigz", "ARVC_niigz", "DCM_niigz", "HCM_niigz"]

  # 提高质量（更慢）
  sampling:
    ddim_steps: 100
```

### Q: CUDA out of memory

减小 `training.batch_size` 或 `config/PatchVolume_8x.yaml` 中的 `batch_size`。

### Q: 训练速度慢

1. 确认使用 `precision: "16"`（混合精度）
2. 增加 `num_workers`
3. 使用更大 `batch_size`

### Q: 数据加载失败

检查 `data.root_dir` 路径和目录结构是否符合 6 级格式。

---

## 附录：配置文件速查表

| 配置文件 | 用途 | 关键参数 |
|----------|------|----------|
| `configs/cardiac_pipeline.yaml` | 主配置文件 | 数据路径、生成参数、训练参数 |
| `config/PatchVolume_8x.yaml` | AutoEncoder Stage1 | batch_size、学习率、checkpoints路径 |
| `config/PatchVolume_8x_s2.yaml` | AutoEncoder Stage2 | batch_size、学习率、resume路径 |
| `config/Singleres_dataset.json` | latent编码配置 | 数据路径 |

---

**知识库状态：** 封存（`knowledge_base.enabled: false`），不影响主流程。
