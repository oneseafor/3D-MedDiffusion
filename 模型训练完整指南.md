# 3D-MedDiffusion 模型训练完整指南

> **目的：** 本文档作为下次换模型训练时的指导手册，确保一次性通过，避免重复踩坑。
>
> **适用场景：** 使用新的心脏 MRI 数据集训练 3D-MedDiffusion 模型
>
> **最后更新：** 2026-05-13

---

## 目录

1. [数据要求与格式](#1-数据要求与格式)
2. [生成目标与输出规格](#2-生成目标与输出规格)
3. [快速适配检查清单](#3-快速适配检查清单)
4. [完整训练流程](#4-完整训练流程)
5. [已知Bug与解决方案](#5-已知bug与解决方案)
6. [测试策略：小批量验证](#6-测试策略小批量验证)
7. [关键参数对照表](#7-关键参数对照表)
8. [代码质量工具建议](#8-代码质量工具建议)
9. [常见错误速查表](#9-常见错误速查表)

---

## 1. 数据要求与格式

### 1.1 目录结构（六级目录）

```
根目录/
├── 中心批次1/
│   ├── 疾病类型1/
│   │   ├── 患者ID1/
│   │   │   ├── 0_final_custom_cine4ch/
│   │   │   │   └── cine_4ch_mid.nii.gz
│   │   │   ├── 0_final_custom_cinesax/
│   │   │   │   ├── cine_sax_down.nii.gz
│   │   │   │   ├── cine_sax_mid.nii.gz
│   │   │   │   └── cine_sax_up.nii.gz
│   │   │   └── 0_final_custom_lgesax/
│   │   │       └── lge_sax_mid6.nii.gz  (或多个 lge_sax_mid*.nii.gz)
│   │   └── 患者ID2/
│   │       └── ...
│   └── 疾病类型2/
│       └── ...
└── 中心批次2/
    └── ...
```

### 1.2 数据尺寸要求

| 模态 | 输入尺寸 | 说明 |
|------|----------|------|
| **cine4ch** | (256, 256, 25) | 25帧电影MRI，单层 |
| **cinesax** | (256, 256, 25) × 3 | 25帧电影MRI，3个切片（down/mid/up） |
| **lgesax** | (256, 256, 6) | 6帧延迟增强MRI |

**关键约束：**
- H/W 必须是 128 的倍数（patch_size=128）
- D 维度：cine=25，lge=6（必须与配置文件一致）
- 文件格式：NIfTI (.nii.gz)
- 轴向顺序：(H, W, D) — 代码会自动检测并转置

### 1.3 疾病类别

| 索引 | 疾病名称 | 目录名 |
|------|----------|--------|
| 0 | ARVC | ARVC_niigz |
| 1 | RCM | RCM_niigz |
| 2 | DCM | DCM_niigz |
| 3 | HCM | HCM_niigz |
| 4 | LVNC | LVNC_niigz |

### 1.4 数据验证命令

```bash
# 检查单个患者数据尺寸
python check_single_patient.py --data-root /path/to/data --patient-id PATIENT_ID

# 验证配置与数据匹配
python verify_config.py --cine
python verify_config.py --lge
```

---

## 2. 生成目标与输出规格

### 2.1 目标

**生成病人级的多序列心脏 MRI 数据，包括：**
- cine4ch：1个文件
- cinesax：3个文件（down/mid/up）
- lgesax：6个文件（mid1-mid6）

**每个患者共 10 个文件**

### 2.2 输出格式

| 模态 | 输出尺寸 | 文件数 | 文件名 |
|------|----------|--------|--------|
| cine4ch | (256, 256, 1) | 1 | cine_4ch_mid.nii.gz |
| cinesax | (256, 256, 1) | 3 | cine_sax_down.nii.gz, cine_sax_mid.nii.gz, cine_sax_up.nii.gz |
| lgesax | (256, 256, 1) | 6 | lge_sax_mid1.nii.gz ~ lge_sax_mid6.nii.gz |

### 2.3 当前限制

**重要：当前模型是分模态训练的，不是统一模型！**

| 模型 | 处理模态 | Latent 尺寸 | 最大输出 |
|------|----------|-------------|----------|
| Cine模型 | cine4ch + cinesax | [8, 25, 16, 16] | 128×128×25 |
| LGE模型 | lgesax | [8, 6, 16, 16] | 128×128×6 |

**无法直接生成 256×256 的完整图像！** 要生成 256×256，需要：
- 训练新模型，使用 `image_size: [25, 32, 32]`（需要 256×256 的 latent 数据）
- 或使用滑动窗口拼接多个 128×128 patch

---

## 3. 快速适配检查清单

### 3.1 环境准备

- [ ] Conda 环境已创建：`conda env create -f 3DMedDiffusion.yaml`
- [ ] 激活环境：`conda activate 3DMedDiffusion`
- [ ] 设置 GPU：`export CUDA_VISIBLE_DEVICES=0`
- [ ] 验证 CUDA：`python -c "import torch; print(torch.cuda.is_available())"`

### 3.2 数据准备

- [ ] 数据目录结构符合六级目录要求
- [ ] NIfTI 文件尺寸正确（256×256×25 或 256×256×6）
- [ ] 疾病目录命名正确（ARVC_niigz, RCM_niigz 等）
- [ ] 每个患者包含所有必需的模态文件

### 3.3 配置文件修改

**必须修改的路径（configs/cardiac_pipeline.yaml）：**

```yaml
data:
  root_dir: "/your/data/root/path"  # ⭐ 修改为你的数据根目录

latent_dir: "/your/output/path/latents"  # ⭐ 修改为你的 latent 输出目录

generation:
  output_root: "/your/output/path/synthetic_data"  # ⭐ 修改为你的生成输出目录
```

**必须修改的路径（config/PatchVolume_8x.yaml 和 PatchVolume_8x_s2.yaml）：**

```yaml
model:
  default_root_dir: "/your/checkpoint/path/patchvolume_stage1"  # Stage 1
  # 或
  default_root_dir: "/your/checkpoint/path/patchvolume_stage2"  # Stage 2
```

### 3.4 参数一致性检查

**必须匹配的参数：**

| 参数 | 位置1 | 位置2 | 必须相等 |
|------|-------|-------|----------|
| latent通道数 | `autoencoder.embedding_dim` (YAML) | `--volume-channels` (CLI) | ✅ |
| 空间尺寸 | `autoencoder.patch_size / downsample[1]` | `diffusion.image_size[1]` | ✅ |
| 深度尺寸 | `autoencoder.patch_depth / downsample[0]` | `diffusion.image_size[0]` | ✅ |
| 类别数 | `diffusion.num_classes` (YAML) | `--num-classes` (CLI) | ✅ |

**验证公式：**
```
image_size[0] = patch_depth / downsample[0]  # 25 / 1 = 25
image_size[1] = patch_size / downsample[1]   # 128 / 8 = 16
image_size[2] = patch_size / downsample[2]   # 128 / 8 = 16
```

### 3.5 输出目录创建

```bash
mkdir -p /your/output/path/{latents,synthetic_data,logs,checkpoints/{patchvolume_stage1,patchvolume_stage2}}
```

---

## 4. 完整训练流程

### 4.1 阶段总览

```
阶段1: AutoEncoder Stage 1 → 学习基础重建
阶段2: AutoEncoder Stage 2 → GAN微调，提升质量
阶段3: 生成 Latent → 编码训练数据为 latent
阶段4: BiFlowNet 训练 → 学习 latent 分布
阶段5: 生成合成数据 → 从噪声生成新数据
```

### 4.2 阶段1：AutoEncoder Stage 1

**目的：** 训练基础自编码器，学习数据压缩和重建

**命令：**
```bash
# Cine 模型
python train/train_PatchVolume.py --config config/PatchVolume_8x.yaml --modality cine

# LGE 模型
python train/train_PatchVolume.py --config config/PatchVolume_8x.yaml --modality lge
```

**预期结果：**
- Loss 持续下降
- 最终 loss ≈ 0.15-0.16
- Checkpoint 保存在 `checkpoints/patchvolume_stage1/`

**关键参数：**
- batch_size: 32
- learning_rate: 3e-4
- discriminator_iter_start: 20000（GAN延迟启动）

### 4.3 阶段2：AutoEncoder Stage 2

**目的：** 使用 GAN 微调，提升生成质量

**命令：**
```bash
# 修改 config/PatchVolume_8x_s2.yaml 中的 resume_from_checkpoint 指向 Stage 1 最优 checkpoint

# Cine 模型
python train/train_PatchVolume_stage2.py --config config/PatchVolume_8x_s2.yaml --modality cine

# LGE 模型
python train/train_PatchVolume_stage2.py --config config/PatchVolume_8x_s2.yaml --modality lge
```

**预期结果：**
- val/recon_loss 比 Stage 1 降低 10-15%
- 最终 loss ≈ 0.08-0.09

**关键参数：**
- batch_size: 8
- learning_rate: 3e-5
- discriminator_iter_start: 5000（GAN在5000步后启动）

**常见问题：**
- 如果 GAN 启动后 loss 上升，增大 `discriminator_iter_start`
- 如果训练过早停止，检查 EarlyStopping 配置

### 4.4 阶段3：生成 Latent

**目的：** 使用训练好的 AutoEncoder 将原始数据编码为 latent 表示

**命令：**
```bash
# Cine latent
python train/generate_training_latent.py \
    --AE-ckpt /path/to/stage2_checkpoint.ckpt \
    --output-dir /your/output/path/latents \
    --modality cine \
    --config configs/cardiac_pipeline.yaml

# LGE latent
python train/generate_training_latent.py \
    --AE-ckpt /path/to/stage2_checkpoint.ckpt \
    --output-dir /your/output/path/latents \
    --modality lge \
    --config configs/cardiac_pipeline.yaml
```

**输出目录结构：**
```
/your/output/path/latents/
├── cine_latent/
│   ├── patient_001.nii.gz
│   ├── patient_002.nii.gz
│   └── ...
└── lge_latent/
    ├── patient_001.nii.gz
    └── ...
```

**验证：** 检查输出文件数量是否与输入患者数量一致

### 4.5 阶段4：BiFlowNet 训练

**目的：** 训练扩散模型，学习 latent 空间的分布

**命令：**
```bash
# Cine BiFlowNet
python train/train_BiFlowNet_SingleRes.py \
    --AE-ckpt /path/to/stage2_checkpoint.ckpt \
    --modality cine \
    --config configs/cardiac_pipeline.yaml \
    --batch-size 4 \
    --epochs 1000

# LGE BiFlowNet
python train/train_BiFlowNet_SingleRes.py \
    --AE-ckpt /path/to/stage2_checkpoint.ckpt \
    --modality lge \
    --config configs/cardiac_pipeline.yaml \
    --batch-size 16 \
    --epochs 1000
```

**关键参数：**
- `--volume-channels 8`（必须等于 embedding_dim）
- `--resolution [25,16,16]`（cine）或 `[6,16,16]`（lge）
- `--num-classes 5`（必须等于实际疾病类别数）

**训练监控：**
- Loss 应持续下降
- 每 500 步保存一次 checkpoint（只保留最近 6 个）
- 每次 checkpoint 保存时生成一个 sample

**终止条件：**
- Loss 收敛（变化 < 0.001 持续 1000 步）
- 或达到设定的 epoch 数

### 4.6 阶段5：生成合成数据

**命令：**
```bash
python evaluation/generate_cardiac_synthetic.py \
    --AE-ckpt /path/to/stage2_checkpoint.ckpt \
    --model-ckpt /path/to/biflownet_checkpoint.pt \
    --output-dir /your/output/path/synthetic_data \
    --mode patch \
    --samples-per-disease 1 \
    --num-classes 7 \
    --seed 42
```

**重要：`--num-classes` 必须为 7（匹配模型结构，不是实际疾病数）**

---

## 5. 已知Bug与解决方案

### 5.1 Latent 目录路径错误

**错误：** `FileNotFoundError: Latent directory not found`

**原因：** `train_BiFlowNet_SingleRes.py` 从 `generation.output_root` 拼接路径，与实际 latent 目录不匹配

**解决：** 在 `cardiac_pipeline.yaml` 添加 `latent_dir` 配置项

```yaml
latent_dir: "/your/output/path/latents"
```

### 5.2 Singleres_dataset 路径后缀重复

**错误：** `IndexError: list index out of range`

**原因：** `Singleres_dataset` 自动添加 `_latents` 后缀，导致路径变成 `.../cine_latent_latents`

**解决：** 使用 `no_suffix=True` 参数

### 5.3 BiFlowNet 维度不匹配（D=25）

**错误：** `RuntimeError: The size of tensor a (25) must match the size of tensor b (32)`

**原因：** D=25 不能被 sub_volume_size=8 整除

**解决：** 已在 `ddpm/BiFlowNet.py` 中添加 padding 支持

### 5.4 einops Shape Mismatch

**错误：** `einops.EinopsError: Shape mismatch, 16 != 12`

**原因：** padding 前后 shape 计算不一致

**解决：** 使用 `padded_shape_upscaled` 替代 `ori_shape_upscaled`

### 5.5 num_classes 不匹配

**错误：** `size mismatch for cond_emb.weight: [7, 288] vs [5, 288]`

**原因：** 模型初始化时使用默认 num_classes=7，但配置文件定义为 5

**解决：** 生成时使用 `--num-classes 7`（匹配模型结构）

### 5.6 输出分辨率只有 128×128

**问题：** 期望 256×256，实际只有 128×128

**原因：** 模型训练时 `image_size: [25, 16, 16]`，解码后最大为 128×128

**解决：** 这是设计限制，不是 bug。要生成 256×256 需要重新训练模型

---

## 6. 测试策略：小批量验证

### 6.1 为什么需要小批量验证？

**直接在完整数据集上训练的风险：**
- 代码 bug 可能导致训练数小时后才报错
- 配置错误可能导致结果不可用
- 路径问题可能导致数据加载失败

**小批量验证的目标：**
- 在 5 分钟内跑通完整流程
- 验证所有配置正确
- 确认输出格式符合预期

### 6.2 构建测试数据集

**创建一个与真实数据结构完全一致的小批量数据：**

```bash
# 创建测试数据目录
mkdir -p test_data/synthetic_center/ARVC_niigz/test_patient_001/{0_final_custom_cine4ch,0_final_custom_cinesax,0_final_custom_lgesax}
mkdir -p test_data/synthetic_center/DCM_niigz/test_patient_002/{0_final_custom_cine4ch,0_final_custom_cinesax,0_final_custom_lgesax}

# 创建测试 NIfTI 文件（使用 Python）
python -c "
import numpy as np
import nibabel as nib
import os

# 创建 cine4ch 数据 (256, 256, 25)
data = np.random.rand(256, 256, 25).astype(np.float32)
img = nib.Nifti1Image(data, np.eye(4))
nib.save(img, 'test_data/synthetic_center/ARVC_niigz/test_patient_001/0_final_custom_cine4ch/cine_4ch_mid.nii.gz')

# 创建 cinesax 数据 (256, 256, 25) x 3
for i, name in enumerate(['cine_sax_down', 'cine_sax_mid', 'cine_sax_up']):
    data = np.random.rand(256, 256, 25).astype(np.float32)
    img = nib.Nifti1Image(data, np.eye(4))
    nib.save(img, f'test_data/synthetic_center/ARVC_niigz/test_patient_001/0_final_custom_cinesax/{name}.nii.gz')

# 创建 lgesax 数据 (256, 256, 6)
data = np.random.rand(256, 256, 6).astype(np.float32)
img = nib.Nifti1Image(data, np.eye(4))
nib.save(img, 'test_data/synthetic_center/ARVC_niigz/test_patient_001/0_final_custom_lgesax/lge_sax_mid6.nii.gz')

print('Test data created successfully!')
"
```

### 6.3 测试流程

**步骤1：验证数据加载**
```bash
python check_single_patient.py --data-root test_data --patient-id test_patient_001
```

**步骤2：验证配置**
```bash
# 修改 configs/cardiac_pipeline.yaml 中的 root_dir 指向 test_data
python verify_config.py --cine
python verify_config.py --lge
```

**步骤3：测试 AutoEncoder Stage 1**
```bash
# 使用小参数快速测试
python train/train_PatchVolume.py --config config/PatchVolume_8x.yaml --modality cine
# 观察前 10 个 step 是否正常
```

**步骤4：测试 Latent 生成**
```bash
python train/generate_training_latent.py \
    --AE-ckpt /path/to/stage1_checkpoint.ckpt \
    --output-dir test_latents \
    --modality cine \
    --config configs/cardiac_pipeline.yaml
```

**步骤5：测试 BiFlowNet 训练**
```bash
python train/train_BiFlowNet_SingleRes.py \
    --AE-ckpt /path/to/stage1_checkpoint.ckpt \
    --modality cine \
    --config configs/cardiac_pipeline.yaml \
    --batch-size 2 \
    --epochs 5
```

**步骤6：测试生成**
```bash
python evaluation/generate_cardiac_synthetic.py \
    --AE-ckpt /path/to/stage1_checkpoint.ckpt \
    --model-ckpt /path/to/biflownet_checkpoint.pt \
    --output-dir test_output \
    --mode patch \
    --samples-per-disease 1 \
    --num-classes 7 \
    --seed 42
```

### 6.4 测试检查点

- [ ] 数据加载无报错
- [ ] 配置验证通过
- [ ] AutoEncoder 训练 loss 下降
- [ ] Latent 文件生成成功
- [ ] BiFlowNet 训练 loss 下降
- [ ] 合成数据生成成功
- [ ] 输出文件尺寸正确
- [ ] 输出文件可以正常打开查看

---

## 7. 关键参数对照表

### 7.1 AutoEncoder 参数

| 参数 | Stage 1 | Stage 2 | 说明 |
|------|---------|---------|------|
| batch_size | 32 | 8 | Stage 2 使用 GAN，显存需求大 |
| learning_rate | 3e-4 | 3e-5 | Stage 2 使用更小学习率 |
| discriminator_iter_start | 20000 | 5000 | GAN 延迟启动 |
| downsample | [1, 8, 8] | [1, 8, 8] | D不降采样，H/W降采样8倍 |
| embedding_dim | 8 | 8 | latent通道数 |
| patch_size | 128 | 128 | 训练patch大小 |
| patch_depth | 25/6 | 25/6 | cine=25, lge=6 |

### 7.2 BiFlowNet 参数

| 参数 | Cine | LGE | 说明 |
|------|------|-----|------|
| image_size | [25, 16, 16] | [6, 16, 16] | latent空间尺寸 |
| volume_channels | 8 | 8 | 必须等于 embedding_dim |
| model_channels | 128 | 128 | 模型通道数 |
| num_classes | 5 | 5 | 疾病类别数 |
| timesteps | 1000 | 1000 | 扩散步数 |
| batch_size | 4 | 16 | 根据显存调整 |

### 7.3 生成参数

| 参数 | 值 | 说明 |
|------|-----|------|
| num-classes | 7 | **必须为7**（匹配模型结构） |
| mode | patch | 只支持 patch 模式 |
| samples-per-disease | 1 | 每个疾病生成样本数 |
| seed | 42 | 随机种子 |

---

## 8. 代码质量工具建议

### 8.1 推荐工具

| 工具 | 用途 | 安装 |
|------|------|------|
| **pyright** | 静态类型检查 | `pip install pyright` |
| **ruff** | 代码风格和错误检查 | `pip install ruff` |
| **mypy** | 类型检查 | `pip install mypy` |
| **pytest** | 单元测试 | `pip install pytest` |

### 8.2 使用建议

**训练前运行静态检查：**
```bash
# 类型检查
pyright train/train_BiFlowNet_SingleRes.py

# 代码风格检查
ruff check train/

# 查找潜在错误
pylint train/train_BiFlowNet_SingleRes.py
```

**是否需要 AST 分析？**

**建议：不需要**

- AST 分析工具（如 tree-sitter, astroid）主要用于代码重构和分析
- 对于训练脚本，静态类型检查和 lint 工具已经足够
- AST 分析的学习成本高，收益有限

**是否需要 RepoAgent？**

**建议：不需要**

- RepoAgent 主要用于生成代码文档
- 已有 OpenWolf 系统（.wolf/）提供类似功能
- 专注于训练流程验证更实际

### 8.3 实用检查脚本

创建一个 `pre_train_check.py` 脚本：

```python
"""训练前配置检查脚本"""
import yaml
import os

def check_config(config_path):
    with open(config_path) as f:
        config = yaml.safe_load(f)

    errors = []

    # 检查路径是否存在
    root_dir = config['data']['root_dir']
    if not os.path.exists(root_dir):
        errors.append(f"数据根目录不存在: {root_dir}")

    # 检查参数一致性
    cine = config['model']['cine']
    ae = cine['autoencoder']
    diff = cine['diffusion']

    expected_h = ae['patch_size'] // ae['downsample'][1]
    expected_w = ae['patch_size'] // ae['downsample'][2]
    expected_d = ae['patch_depth'] // ae['downsample'][0]

    if diff['image_size'] != [expected_d, expected_h, expected_w]:
        errors.append(f"image_size 不匹配: 期望 [{expected_d}, {expected_h}, {expected_w}], 实际 {diff['image_size']}")

    if ae['embedding_dim'] != diff['in_channels']:
        errors.append(f"embedding_dim ({ae['embedding_dim']}) != in_channels ({diff['in_channels']})")

    if errors:
        print("❌ 配置检查失败:")
        for e in errors:
            print(f"  - {e}")
        return False
    else:
        print("✅ 配置检查通过")
        return True

if __name__ == "__main__":
    check_config("configs/cardiac_pipeline.yaml")
```

---

## 9. 常见错误速查表

| 错误信息 | 原因 | 解决方案 |
|----------|------|----------|
| `FileNotFoundError: Latent directory not found` | latent_dir 路径错误 | 在 YAML 中添加 `latent_dir` 配置 |
| `IndexError: list index out of range` | 路径后缀重复 | 使用 `no_suffix=True` |
| `RuntimeError: size of tensor a (25) must match size of tensor b (32)` | D维度不能被8整除 | 已修复，确保使用最新代码 |
| `einops.EinopsError: Shape mismatch` | padding前后shape不一致 | 已修复，确保使用最新代码 |
| `size mismatch for cond_emb.weight` | num_classes不匹配 | 使用 `--num-classes 7` |
| `No such file or directory: .../latest_checkpoint.ckpt` | checkpoint路径错误 | 检查 checkpoint 是否存在 |
| `AttributeError: 'Singleres_dataset' has no attribute 'file_num'` | JSON路径错误 | 确保 root_dir 以 .json 结尾 |
| 输出只有 128×128 | 模型设计限制 | 这是预期行为，不是bug |

---

## 附录A：完整命令参考

### 环境准备
```bash
conda activate 3DMedDiffusion
export CUDA_VISIBLE_DEVICES=0
mkdir -p /your/output/{latents,synthetic_data,logs,checkpoints/{patchvolume_stage1,patchvolume_stage2}}
```

### 完整训练流程（Cine）
```bash
# 1. AutoEncoder Stage 1
python train/train_PatchVolume.py --config config/PatchVolume_8x.yaml --modality cine

# 2. AutoEncoder Stage 2
python train/train_PatchVolume_stage2.py --config config/PatchVolume_8x_s2.yaml --modality cine

# 3. 生成 Latent
python train/generate_training_latent.py \
    --AE-ckpt /path/to/stage2_checkpoint.ckpt \
    --output-dir /your/output/latents \
    --modality cine \
    --config configs/cardiac_pipeline.yaml

# 4. BiFlowNet 训练
python train/train_BiFlowNet_SingleRes.py \
    --AE-ckpt /path/to/stage2_checkpoint.ckpt \
    --modality cine \
    --config configs/cardiac_pipeline.yaml

# 5. 生成合成数据
python evaluation/generate_cardiac_synthetic.py \
    --AE-ckpt /path/to/stage2_checkpoint.ckpt \
    --model-ckpt /path/to/biflownet_checkpoint.pt \
    --output-dir /your/output/synthetic_data \
    --mode patch \
    --num-classes 7
```

---

## 附录B：配置文件模板

### configs/cardiac_pipeline.yaml（关键部分）

```yaml
data:
  root_dir: "/your/data/root"  # ⭐ 修改

latent_dir: "/your/output/latents"  # ⭐ 修改

model:
  cine:
    autoencoder:
      downsample: [1, 8, 8]
      embedding_dim: 8
      patch_size: 128
      patch_depth: 25
    diffusion:
      image_size: [25, 16, 16]
      num_classes: 5

generation:
  output_root: "/your/output/synthetic_data"  # ⭐ 修改
```

---

**文档完成时间：** 2026-05-13
**维护者：** Claude Code Assistant
**版本：** 1.0
